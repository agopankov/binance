apiVersion: apps/v1
kind: Deployment
metadata:
  name: binance-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: binance-client
  template:
    metadata:
      labels:
        app: binance-client
    spec:
      serviceAccountName: binance-app-sa
      containers:
        - name: binance-client
          image: 174907374174.dkr.ecr.eu-west-2.amazonaws.com/binance-client:latest
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "aws-secrets-manager"
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: binance-client-config